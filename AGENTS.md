## Cursor Cloud specific instructions

- This checkout currently contains only `README.md`; no application manifests, setup scripts, devcontainer config, or service definitions were found.
- The Cursor Cloud startup update script is intentionally `true` because there are no repository dependencies to refresh at this commit.
- If application code is added later, update this section and the startup update script to reference the new development setup commands.
